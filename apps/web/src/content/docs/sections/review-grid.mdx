---
title: Review Grid
description: TODO.
---

<ComponentPreview name="review-grid-demo"/>

## Installation

<Steps>

<Step>Add the following code to your `tailwind.config.js` file:</Step>

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./src/**/*.{ts,tsx}"],
  darkMode: "class",
  theme: {
    // rest of the code
  },
 keyframes: {
        'fade-in': {
          from: {
            opacity: '0',
          },
          to: {
            opacity: '1',
          },
        },
        marquee: {
          '100%': {
            transform: 'translateY(-50%)',
          },
        },
        flashing: {
          '0%, 100%': { opacity: '0.2' },
          '20%': { opacity: '1' },
        },
      },
      animation: {
        'accordion-down': 'accordion-down 0.2s ease-out',
        'accordion-up': 'accordion-up 0.2s ease-out',
        marquee: 'marquee var(--marquee-duration) linear infinite',
        'fade-in': 'fade-in 0.5s linear forwards',
        flashing: 'flashing 1.4s infinite linear',
      },
      // rest of the code
};
```

<Step>Copy and paste the following code into your project.</Step>

```text
components/evaui/review-grid.tsx
```

<ComponentSource name="review-grid" />

</Steps>

## Props

| Prop        | Type   | Description                             | Default |
| ---------   | ------ | ---------------------------------       | ------- |
| className   | string | The class name for the component        | ""      |
| icon        | node   | Icon to identify each card in the grid  | ""      |
| title       | string | Card title                              | ""      |
| description | string | Card description                        | ""      |
| header      | node   | JSX component for above the title       | ""      |
| color       | string | HEX color around the cursor when hover  | "fff"   |
